- @title = h "Delete Resume - #{@resume.name}"
.header
  %ul.toolbar
    %li
      %a{:href => "/resumes/#{@resume.id}.html"} « Return
  %h1 Delete Resume
  %h2 #{@resume.name}

.content  
  %h4 Are you sure you want to delete this resume?
  %form{:action => "/resumes/#{@resume.id}", :method => "post"}
    %input{:type => "hidden", :name => "_method", :value => "delete"}
    %button{:type => "submit"} YES
    
.clear